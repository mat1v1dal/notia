node_modules/
dist/
*.tsbuildinfo

# secretos — nunca versionar
.env
.env.*
!.env.example

# datos locales
pgdata/
evolution-data/

# la PWA compilada la sirve la API; se genera con pnpm build:web
packages/api/public/
